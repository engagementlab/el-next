import { ReactNode } from 'react';
declare type Props = {
    children: ReactNode;
    title: string;
    description: string;
};
export declare const Layout: ({ children, title, description, }: Props) => JSX.Element;
export {};
